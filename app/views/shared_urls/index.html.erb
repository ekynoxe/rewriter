<h1>Your bookmarks<span class="orange">:</span></h1>
<div id="box">
<% @shared_urls.each do |url| %>
<p>
	<%= link_to url.full_url, root_url()+(url.short_url || '') %> <small>(<%= root_url()+(url.short_url || '') %>)</small>
</p>
<% end %>
</div>